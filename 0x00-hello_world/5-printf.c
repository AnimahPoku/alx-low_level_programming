#include <stdio.h>

/**
 * main - a function that prints a line with the printf function
 * Return:0 if successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
